tools:
  versioner:
    exec: ["sh", "-c", "echo v1.0.0"]
    extract: 'v[0-9]+\.[0-9]+\.[0-9]+'

commands:
  - name: producer
    cmd: ["sh", "-c", "cp seed.txt a.txt"]
    inputs: ["seed.txt"]
    outputs: ["a.txt"]
    tools: [versioner]

tools:
  versioner-b:
    exec: ["sh", "-c", "echo v1.0.0"]
    extract: 'v[0-9]+\.[0-9]+\.[0-9]+'

commands:
  - name: gen
    cmd: ["sh", "-c", "mkdir -p internal && cp input.txt internal/foo.gen.txt"]
    inputs: ["input.txt"]
    outputs: ["internal/foo.gen.txt"]
    tools: [versioner-b]

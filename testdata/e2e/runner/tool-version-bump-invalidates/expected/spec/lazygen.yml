commands:
  - name: copy
    cmd: ["sh", "-c", "cp input.txt output.txt; printf x >> ../marker.txt"]
    inputs: ["input.txt"]
    outputs: ["output.txt"]
    tools:
      - exec: ["sh", "-c", "echo v2.0.0"]
        extract: 'v[0-9]+\.[0-9]+\.[0-9]+'

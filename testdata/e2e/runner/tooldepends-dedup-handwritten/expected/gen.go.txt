package gen

// Suffix is what the fixture generator appends; gen-source (re)generates
// this file, so on a clean tree it does not exist and the tool below cannot
// compile until gen-source has run.
const Suffix = "generated"
